<?php
    $isSaLanguage = $this->isSaLanguage();
    $cartLogo = $isSaLanguage ? 'cart_ar' : 'cart';
    $cartLogoUrl = $this->getViewFileUrl(sprintf('Tamara_Checkout::images/%s.svg', $cartLogo));
    $titleCartLogo = __('Buy now pay later in 30 days');
    $saLangClass = $isSaLanguage ? ' sa-language' : '';
?>
<div class="tamara-info-popup">
    <div class="button-popup">
        <div class="wrapper">
            <button class="modal-toggle">
                <img width="295" height="50" src="<?php echo $cartLogoUrl ?>" alt="Tamara - <?php echo $titleCartLogo; ?>"/>
            </button>
        </div>
        <div class="popup-tamara-config<?php echo $saLangClass; ?>">
            <?php
            echo $this->getLayout()->createBlock('Magento\Cms\Block\Block')->setBlockId('tamara_cms_block_info')->toHtml();
            ?>
        </div>
    </div>
</div>
<script type="text/javascript">
    require(['jquery', 'jquery/ui'], function($){
        $(document).ready(function () {
            $('.modal-toggle').on('click', function(e) {
                e.preventDefault();
                $('.modal').toggleClass('is-visible');
            });
        });
    });
</script>
